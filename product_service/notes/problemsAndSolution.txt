1.Issue with cdk not loading .env files when i run "npm run dev" script

* Opened cdk.out/ProductServiceStack.template.json and find all resources of "Type": "AWS::Lambda::Function". Note the keys (e.g., "ProductLambda", "CategoryLambda" from your logs—these are the function logical IDs).
* Created a file env.json in your project root with this structure (replace with your actual function names; use the same MONGODB_URI for all):

{
  "ProductServiceproductLambda46558C21": {
    "MONGODB_URI": "mongodb://localhost:27017/product_service_db"
  },
  "ProductServicecategoryLambda4AADC383": {
    "MONGODB_URI": "mongodb://localhost:27017/product_service_db"
  },
  "ProductServicedealsLambda594E45C8": {
    "MONGODB_URI": "mongodb://localhost:27017/product_service_db"
  },
  "ProductServiceimageUploadLambdaFB6CC065": {
    "MONGODB_URI": "mongodb://localhost:27017/product_service_db"
  },
  "ProductServicemsgQueueLambda77C580EE": {
    "MONGODB_URI": "mongodb://localhost:27017/product_service_db"
  }
}

* Updated the package.json "dev" script:
"dev": "cdk synth && sam local start-api -t cdk.out/ProductServiceStack.template.json --env-vars env.json"

cdk.json
"app": "npx ts-node -r dotenv/config --prefer-ts-exts bin/your-app.ts"