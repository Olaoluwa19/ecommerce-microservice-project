# version: "3.9"
services:
  postgress:
    image: postgres:latest
    restart: always
    env_file:
      - .env # This loads all vars from .env into the container
    ports:
      - "5432:5432"
    volumes:
      - db:/var/lib/postgresql/data

volumes:
  db:
    driver: local
